Download the TRAIN dataset. 
dataset_creator: turns the pictures dataset with 8 folders corresponding to 8 poses into a csv files with the 14 angles we are computing. 
training: trains and saves the model
